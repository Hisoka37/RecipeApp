<section>
  <div>
    <%= link_to "Add Food", new_food_path, class: "edit-link" %>
  </div>
  <div>
    <table class="table">
      <thead>
        <tr>
          <th>Food</th>
          <th>Measurement Unit</th>
          <th>Price</th>
          <th>Actions</th>
      </thead>
      <tbody>
        <% @foods.each do |food| %>
        <tr>
          <th><%= food.name %></th>
          <td><%= food.measurement %></td>
          <td><%= number_to_currency(food.price) %></td>
          <td>
            <%= button_to "Delete", food_path(food), class: "border border-light rounded-pill", method: :delete, data: { confirm: "Are you sure about delete #{food.name}?" } %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</section>